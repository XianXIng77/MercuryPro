# Local Turnstile Solver (subproject of grokcli-2api)
quart>=0.19
# Keep the solver and registration browser on the same launcher generation.
# Camoufox browser v152 requires the 0.5 launcher/cache layout.
camoufox==0.5.4
patchright>=1.50
rich>=13
requests>=2.31
python-dotenv>=1.0
